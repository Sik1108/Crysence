# MiniMax API（请只配置在服务器环境，不要写入前端代码）
MINIMAX_API_KEY=your_minimax_api_key
MINIMAX_API_BASE=https://api.minimax.io/v1
MINIMAX_TEXT_MODEL=MiniMax-M2.7

# 可选。正式部署会从 HTTPS 请求自动识别公网域名；仅反向代理无法传递正确 Host 时才填写。
PUBLIC_BASE_URL=

HOST=0.0.0.0
PORT=4174
